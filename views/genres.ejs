<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Genres</title>
  </head>
  <body>
    <h1>Here are all the genres:</h1>

    <% data.forEach((curr)=>{ %>
    <div style="display:flex;flex-direction: row;">
      <h2><a href="/genres/<%= curr.name %>"><%= curr.name %></a></h2>
    <form action="/genres/delete/<%= curr.name %>" method="post"><button>Delete</button></form>
    
    </div>
    <%}) %>
    <a href="/genres/add">Add genres</a>
    <a href="/">Back to main page</a>
  </body>
</html>
